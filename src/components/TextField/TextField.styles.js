import { css } from "@emotion/core";

export const textFieldStyles = {
  base: css`
    && label {
      position: unset;
      z-index: unset;
      transform: unset;
      margin-bottom: 8px;
    }

    && legend {
      width: 0 !important;
    }
  `
};
