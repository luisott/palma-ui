import { css } from "@emotion/core";

export const appBarStyle = css`
  && {
    width: 100%;
    overflow: inherit;
    height: 100%;
    align-self: flex-end;

    .MuiTabs-indicator {
      background-color: white;
    }
  }
`;
